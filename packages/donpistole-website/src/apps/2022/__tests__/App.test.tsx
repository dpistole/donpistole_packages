import React from 'react';
import { App } from '../index';
import { render } from '../../../test-utils';

describe('2022 App', () => {
  it('renders successfully', () => {
    render(<App />);
  });
});
